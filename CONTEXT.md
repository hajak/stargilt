# StarGilt (né Aetherforge) — deckbuilder

- **Live game = the Chapters build** (promoted 2026-07-12). **LIVE = `chapters · v0.11.7`** (deployed 2026-07-18). **v0.11.7 = polish:** (1) **fixed a misleading focus badge** — a v0.11.5 free-focus-engine (e.g. Umbral Conclave at cost 0 ❖) still rendered a "❖ 0" cost pill + the red `needsfocus` ember edge, reading as if it cost focus. `costsFocus` (buildCardEl) now keys on the ACTUAL `focusCostOf(def)>0` (was `!isFreePlay(def)`, which missed the freeFocusEngine rule), so free cards show no ❖ badge/edge while real-cost cards — including rare/mythic engines like Nightmare Vintner — still do. (2) **the game-start Emberheart Charm message holds ~3.6s** — `slamText` gains a `hold` option that lengthens its animation-duration (the keyframe holds opacity 16%→78%, so a longer total = a longer read); the charm instruction passes `hold:3.6`. **v0.11.6 = START ARMED (deployed 2026-07-18). **v0.11.6 = START ARMED with a visible Emberheart Charm.** The invisible free Act-1 first-miss mercy (`state.act1Mercy`) is replaced by a REAL Emberheart Charm mounted on the bench from turn 1, announced at game start ("THE FORGE ARMS YOU · AN EMBERHEART CHARM · IT FORGIVES YOUR FIRST MISSED TITHE"). It shatters to forgive the first missed tithe ANYWHERE (no longer Act-1-only), then you buy more at the Bazaar as before. Cleaner + more honest than a hidden flag, and it shows in the `ch_turn.charms` telemetry from t1. Wiring: `boot()` mounts `state.relics=[makeInstance(DB.emberheart)]` + `renderBench()` for a fresh chapter run (a CONTINUE's `restoreRun` overwrites relics with the saved bench, so no double-mount); the miss/death path drops the `act1Mercy` free-pass branch and goes straight to the charm-shatter (`const charm=state.relics.find(r=>r.def.mercyCharge&&!r.shattered); if(!charm)return gameOverSequence`); `state.act1Mercy` removed from init/saveRun/restoreRun (difficultyConfig keeps a literal `act1Mercy:true` — still accurate: the run starts with a forgiveness). Tests: v084 mercy test rewritten to the charm behavior (starts with 1 charm, first miss shatters+continues, second miss ends the run); v085 death test clears the gifted charm first; full battery green. **v0.11.5 = CONSISTENCY fixes (deployed 2026-07-18). **v0.11.5 = CONSISTENCY fixes (the deaths were VARIANCE, not the curve).** The v0.11.3 variance panel proved it: by Act 2 the tightness was already in the ideal band (1-2×), and the deaths were bricked hands (DEAD HAND tags) + a focus-lock the player named ("focus cards cost focus"), not a too-hard curve. Root causes: only ONE early draw source (a single Tidewake) and focus-engine cards that cost the very ❖ they provide (unplayable at 0 ❖, e.g. under The Sealed Hearth −1❖). Fixes: (1) **FOCUS-LOCK**: common/uncommon focus engines (`gains.focus>0`) now play FREE — `focusCostOf` gains a `freeFocusEngine` branch (rare/mythic keep their cost; also now respects an explicit `focusCost:0`, which the old `||` fell through). (2) **2ND DRAW**: the starter is `['tidewake','tidewake','kindling']` (13 cards) — a no-Tidewake opening no longer means no dig. (3) **DEAD-HAND MULLIGAN**: `mulliganDeadHand(dealN)` — after the opening deal, if the hand has no draw/focus enabler (`openSnap.enab===0`, the exact dead hand that ended runs), it shuffles back and redraws ONCE ("A DEAD HAND — THE FORGE RELENTS"; guarded by `state.mulliganed` per turn; wired at the new-turn deal, the resumed-run deal, and turn-1). (4) **TIGHTER START**: `GRACE_FLOOR` 0.5→0.62 lifts ONLY the earliest turns (t1 ~6→7, converges to full demand by t8) to trim the loose 5× coast; the mid is untouched (self-registers via `difficultyConfig().graceFloor`). Deliberately NO demand-rate change — the mid is already correct; deaths move later as bricks stop. Tests: focus-engine costs (free vs rare/mythic-kept), 2 Tidewakes, mulligan fires on a forced dead hand + redraws an enabler, t1 demand tightened / mid untouched; full battery 223 green. **v0.11.4 = BOSS ROSTER REDESIGN (deployed 2026-07-18). **v0.11.4 = BOSS ROSTER REDESIGN for mechanical variety.** Bosses were samey (3 of 8 — Sealed Hearth/Molten Vise/Masterwork — just cut ❖) and the t30 boss (The Guttering, `comboMult:0` chain-dead at 2.4×) was a hard wall for combo decks. Now each of the 8 Aspects has a DISTINCT bite; only bosses 1 & 8 keep a focus penalty. Changes: (act 3) **The Tarnish** — NEW `dampColor:{color,mult}` mechanic: one RANDOM colour scores 50% this turn (picked at boss setup from ember/tide/verdant/umbra/aurel — excludes ash; sets `state.twist.color` so the board tints to it; applied in `starsFor` as the single site so it shows on the per-card float and keeps projection===tally; replaces Molten Vise's −2❖). (act 5, the too-hard t30) **The Silting** — `drossSeed:2` deals 2 Dross into the hand, permanently silting the deck; `demandMult` 2.4→2.2 (t30 demand ~8.5k→~7.8k); replaces the chain-dead Guttering. (act 6) **The Hollow Forge** reframed — `allStarMult:0.6` "every card's ★ worth 60%" (the requested "all cards score less"; was `multDamp:0.6` — same net linear cut, clearer copy). Kept distinct: −1❖ (1), draw−2 (2), 15% ★ skim (4), ≤3 plays (7), −2❖+skim capstone (8). **Retired**: `comboMult` (chain-dead) and `multDamp`. Presentation: `bossLedger` gains bite rows for `allStarMult<1` and `dampColor` (names the colour via COLORS); the new bosses get live-HUD strings (The Tarnish names the picked colour). admin `BOSS_BY_ACT` mirror updated (act3 Tarnish, act5 Silting) for death-location labels. The self-registered `difficultyConfig().bossMults` reflects act-5 2.4→2.2, so the Versions registry diffs it. NOTE: "too easy until 30" was a pre-v0.11.3 run — v0.11.3 already tightened mid demand; this pass is variety + the t30 ease, not a blanket bump. Tests: each mechanic + ledger/live + invariant + t30 demand headless; boss intro screenshot; full battery 223 green. **v0.11.3 = richer per-turn difficulty telemetry (deployed 2026-07-18). **v0.11.3 = richer per-turn difficulty telemetry (design-vs-luck) + a mid-game tuning pass.** The Difficulty tab could show a turn's tightness but not WHY a turn was easy/hard — Hampus's run died t31 at 0.2× (demand 3.4k < the t30 boss) to a post-boss diluted deck + a dead opening hand, indistinguishable in the data from genuine difficulty. Now: (1) **`ch_turn` logs the hand/deck state**: `deckN` + `drossN` (bloat/dilution from the census `[...deck,...discard,...hand,...played]`), the OPENING-hand composition `{openN,openEnab,openPlay,openDead}` captured at the DEAL seam via `captureOpenHand()` (deal-time, not the tally — the tally hand is the residue; fired after the new-turn deal+drossSeed ~5314 and the two boot deals; **`openEnab===0` = the "dead hand"**), `focusLeft`/`buysLeft`, `played`/`combo`, `charms` (unshattered mercyCharge count at judgment). Storage automatic (server keeps `extra` verbatim). (2) **Server**: `/api/admin/difficulty` `byTurn` adds `medDeckN`/`medDrossN`/`deadHandRate`/`avgFocusLeft` + a miss-cause tally `{dead,diluted,hard}` (dead = openEnab 0; diluted = drossN/deckN≥.25; else hard); `/api/admin/balance` turns carry the raw fields. (3) **Admin full variance analysis** on the Difficulty tab: a "Why — deck dilution & dead hands" panel (`renderVariance` — median deck-size + Dross lineChart, a `clearChartPct` dead-hand-rate bar row), run-strip cells tagged by CAUSE (`turnCause()` → dead hand / diluted / genuinely hard, so a 0.2× t31 reads as bad luck not bad balance), a post-boss watch (turns 7/13/19/25/31/37/43 — the boss Dross-injection dilution spike), and an opening-hand one-liner (% of hands with no enabler). (4) **Mid-game tuning pass** (turns 1-29 read too-easy at 2-6×): `DEMAND_RATE` **1.28→1.30** steepens the geometric pre-knee climb — the exponent concentrates the increase on the MID (t12 283→335, t18 1439→1872, ~+18-30%) while the early stays grace-cushioned (**boss 1 only +8%**); `DEMAND_LATE_RATE` **1.12→1.11** HOLDS the summit (t48 74,550→74,142). Provisional first pass; partially offsets v0.11.1's give-back for strong players (newcomers keep mercy + star-boost); the new telemetry drives the real per-skill tuning next. Self-registered via `difficultyConfig()` so the Versions registry shows the diff. Tests: new-mechanics headless (config rates, telemetry fields, dead-hand detection, server aggregation, admin variance panel + cause tags) + updated demand assertions in v0110/v090 — full battery green. **v0.11.2 = VERSION-AWARE DIFFICULTY ANALYTICS (deployed 2026-07-18). **v0.11.2 = VERSION-AWARE DIFFICULTY ANALYTICS + the game self-registers its balance.** The Forge Console had conflated every balance version into single meaningless numbers (a v0.6 win counted as a v0.11 win; the Difficulty tab's demand line was drawn from whichever run was processed last while median score pooled every version). Fixed by sorting difficulty by `build` while keeping engagement global. (1) **SELF-REGISTER**: the game emits a `ch_config` beacon once per real run — `Balance.config(difficultyConfig())` at boot (chapterMode-gated, BEACON_OFF-honored) — carrying every difficulty knob (demandBase/Rate/knee/lateRate/grace, multCap/capPerBoss/xCap, chain/echo/tribalRate, starBoost, startGold/bossGold, the 8 boss demandMults, act1Mercy). Future versions now auto-document their balance; NO structured per-version record existed before (only prose changelog + git). (2) **SERVER**: new `GET /api/admin/difficulty` groups `ch_turn`/`ch_run` BY BUILD → per-version `{byTurn:[{t,dem,med,p25,p75,cleared,alive}], runCount, wins, deaths, config}` (aggregated, so no 60-run cap); `GET /api/admin/balance` gains `?build=` for the run strips. (3) **DIFFICULTY TAB** is a single-version deep dive: defaults to the LATEST build (semantic-sorted dropdown of every version with data), draws the demand curve EXACTLY from that version's config (killing the last-writer `b.dem` bug), and shows a difficulty-settings panel with a delta-vs-previous highlight. (4) **NEW VERSIONS TAB**: a registry table of every build's knobs — git-exact for v0.8.3+, prose-backfilled + `approx` flag for v0.7.x (`HISTORICAL_VERSIONS` const in admin.html; live `ch_config` telemetry wins when present; `configFor()` falls back to the newest entry ≤ a build so patch versions inherit) — with changed cells auto-highlighted vs the version below, plus a **tightness overlay** (median score ÷ that version's demand, per turn — version-normalized difficulty, 1.0 = just cleared) comparing any picked builds. (5) **PULSE**: engagement stays global (players/active/playtime/sessions/countries — version-invariant); the version-DEPENDENT stats (Forged/win %, median depth, the Game funnel — now per-RUN) scope to a build selector defaulting to latest, backed by `aggregate.js overview.byBuild`. Also: `playerDetail.runs[]` carries `build`. Tests: new-mechanics headless (difficultyConfig shape, /api/admin/difficulty medians + config merge, balance?build=, Difficulty/Versions/Pulse driven) + admintest updated (6 tabs, per-run game funnel) — full battery green. **v0.11.1 (playtest tuning + polish)** (deployed 2026-07-18). **v0.11.1 = playtest tuning + debrief polish:** (1) **The Victory Debrief gains a mystical animated backdrop** — `#db-cosmos`, a full-viewport canvas behind `.db-doc` (z0; embers z1; doc z2) drawing three counter-rotating forge-sigil rings + a 48-star twinkling gold constellation that drifts gently upward, on its OWN self-sleeping RAF (`Cosmos` module; started in `openDebrief`, sleeps when `#debrief` loses `.on`); faint by design (canvas opacity .6 + low per-element alpha, `globalCompositeOperation:'lighter'`); `REDUCED_MOTION` → one static frame. (2) **Balance give-back for "slightly too hard" (v0.11 mult nerf) — via STARS + GOLD, deliberately NOT mult** (the capped-mult tension stays): `STAR_BOOST=1.06` (beside the mult consts) — each PLAYED card scores 6% hotter, applied per-card in BOTH `computeScore` (`Σ starsFor·sm·STAR_BOOST`) AND the end-turn tally (`g=gBase·sm·STAR_BOOST`, `gShown=round(g)`) so the climbing meter, the floats, and the `base × mult = final` equation stay in lockstep — **projection===tally invariant intact** (verified: glory gained === computeScore().final); the `⚡` synergy flag still compares the RAW `gBase>glory` (a global boost must not light every card). Per-boss gold reward `+5→+6`. (3) **Opening Bazaar guarantees a Dross-for-power ware** — `pickReverie` adds `d=>!!d.gainSlag&&(d.cost<=4||(d.focusCost||0)>0)` as the FIRST of the 7 guaranteed food groups (market stays 7 wide, no random-fill slot) so a strong cheap/low-focus card that seeds a Dross (Pyre Bloom c4/❖1/4★, Silt Dredger, Ashgilt Idol) is offerable from turn 1 — the burn loop shows up early. (4) **Start with 6 gold** (`state.gold` 3→6). Tests: full battery 223 assertions green + a new-mechanics headless pass (start-gold 6, STAR_BOOST 1.06, base-★ boosted & rounded, boss gold +6, opening Dross guarantee 100%, cosmos draws+animates+self-sleeps, invariant holds). **v0.11.0 = NINE PLAYTEST FIXES + ENDLESS REMOVED + THE VICTORY DEBRIEF.** Playtest batch: (1) **VIEW YOUR CARDS** ghost buttons on the Boon + Master-Relic overlays — `openPileView` allows a *suspended* reward (`pvReadonly`: its promise holds endTurn, so the state is stable; read-only = no forge/burn rows, zoom passes `{def}` only; `#pileview` z 165→**172**, above boon 166/mrdraft 170; Esc chain already closed pileview before the boon). (2) Piles `top` 76→96px (clear of END TURN; the trauma-shake ROTATION displaces the lower piles more — pilejumptest sway tolerance widened to 22px, layout-offset + exact-return invariants unchanged). (3) burnMode: the pile-view card BODY now zooms through a shared `zoom()` honoring pvReadonly/burnMode/forge — the burn-less-zoom bug (was hardcoded `forge:true`). (4) Boon/mrdraft backdrops **opaque + blurred** (.78/.82 center alpha + backdrop-filter blur 7/8px). (5) THE PHANTOM CLOSE: `#inspect` (z170, earlier in DOM) painted UNDER `#mrdraft` (z170) — the right-click relic zoom showed only its CLOSE through the translucent backdrop and clicks landed on the draft; `#inspect`→**175** (ties with the JS-appended coachchip resolve correctly); + explicit **SKIP CLAIM** (`mrResolve`, click-only — deliberately NOT Escape, a habitual double-Esc must not eat a Master Relic; caller ignores the resolution so a no-mount skip is safe). (6) **Admin liveness**: `won===null` only means "no ch_run beacon" — abandoned tabs never send one; badges now age-gate (`STALE_MS` 10 min → gray ABANDONED tN) and non-ended rows print `Σ turns[].score` (real glory, was `endGlory||0` = 0★) + `seen <ago>`. (7) **Admin WATCH**: new per-turn `ch_board` beacon beside `Balance.turn` (`boardSnap()` — SELF-CONTAINED card chips `{i,n,c,r,k,g,f?}` because the admin has no card DB; hand≤20/played≤24 caps keep it <8KB) → `GET /api/admin/board?cid=` (own route: `/api/admin/events` whitelists extra and would strip it) → `#spectate` panel, own 12s poll, zero animation. (8) **MULT NERF**: `CHAIN_RATE=.18/ECHO_RATE=.10/TRIBAL_RATE=.20` — constants shared by `computeScore` AND the reveal (the reveal hardcoded `.2*effCombo()` twice; they must never desync); `multDouble` +3→+2.5 (compute+reveal+slam+tooltip); card `m`: Verdant Bloom .15/Grove Warden .10/Compound .25/Prism .20/Catalyst .15/Second Sun 1.2; `relicMult` −20% (.15/.25/.25/.4/.65; crowns .8/1.2/2); `emberBloom` .12/.25/.4; **compensation** `DEMAND_LATE_RATE` 1.13→**1.12** (t24 3,223/t30 7,271/t36 15,547/t42 33,048/t48 74,550 — Acts 1-3 byte-identical). Net: t9-18 ~15-18% harder (the ask), t36+ neutral. `forgedDefOf` doubles `m` so nerfs flow to forged copies. (9) **THE BAZAAR RESTOCKS after every boss clear** (`evolveMarket()` + `evolveRack()` if a shelf emptied, in the boss-clear block) — the old restock ran ONLY in the rank-up loop and max rank (30k glory) lands ~Act 3-4, freezing the shop for the rest of the game; SOLD slots now read "new wares after the boss". **ENDLESS REMOVED** (design: beating the Masterwork ENDS the game): `state.endless`/`pendingVictory`/`endlessDemand()`/`ENDLESS_GROWTH`/Heat saw-tooth all deleted; the t48 kill plays the hard-stop + slam, silently syncs final rank (no ceremony), removes the rush listener, and `await runVictory(); return;` — **no sweep, no commissions, no turn 49, no save, no redeal** (the old flow resolved the turn into a dealt turn-49 Endless board before the ceremony); pre-v0.11 endless saves are RETIRED in `loadSave()` (removeItem + null — their score is already on the roll). **THE VICTORY DEBRIEF** (`#debrief` z265; `#gameover` is death/trial-only now): full-screen forge-intelligence dossier — `runVictory` captures `Balance.rows`/`cards` BEFORE `Balance.end(true)` wipes them (order is load-bearing), `clearRun()`, new `game_won` telemetry, `finishScores(targetEl)` refactor (null = record+submit only; returns `{best,shattered,entry,top,submitP}`); modules: hero tiles (glory count-up, rank, active time, honor-roll placement upgraded async from `submitP`), **THE RUN CURVE** (hand-rolled log-Y SVG `debriefCurveSVG`: gold score path + gradient fill, dashed ember demand, act numerals, boss diamonds filled=cleared/hollow=forgiven, t48 glow), **THE EIGHT TRIALS** ledger (asked/paid/margin%, min-margin row = THE CLOSEST SHAVE, FORGIVEN tags), **ENGINE AUTOPSY** (peak turn = bestTurnGlory, longest chain = new `state.maxCombo` tracked at the real play site NOT the projection sim, final base×mult, log-share bar, MULT ENGINE/TWIN-HAMMERED/STAR-WROUGHT verdict), **THE DECK AS IT STOOD** (deck+discard+hand+played pool — played is still on the table, rarity/⁂ histogram, most played/bought/forged/burned via cards.names), **THE BENCH** (SPENT strikethrough), NEW RUN + VIEW HONOR ROLL (hiscore z340 opens above); MASTERWORK COMPLETE seal stamps at +600ms with one sub-thud; dossier figures in `--mono` tabular-nums; display font ONLY on title+seal. Tests: `v0110test.mjs` (21) + full battery 223 assertions. **v0.10.0 = THE DOPAMINE & JUICE OVERHAUL** — the full `tasks/dopamine-plan.md` build (all 6 sections; critique bugs B1-B4 respected), plus honor-roll tabs + a player-facing ANALYTICS panel. **§0 Heat spine**: a global intensity signal — `Heat.run` (slow 0-1, recomputed via `Heat.reset()` at each new turn / boss clear (dips ×.4 for the draft) / rank-up; Endless breathes per 6-turn saw-tooth instead of pinning (B4); trial pins .05), `Heat.surge` (fast, `spike()`d by big turns), `Heat.timeScale` (hit-stop) — on its OWN self-sleeping RAF (B1: no always-on loop exists anywhere). `Heat.fxScale` = the player SCREEN FX knob. **§1 evolving music**: `Music.onHeat(run)` opens pad/bass filters (500+3500r / 420+2200r via setTargetAtTime — sums safely with the pad LFO), density gates in `schedule()` (hats .5+.42r, stabs .35+.4r, noodle .04+.14r), BPM ramps 104→146 by act (~3 BPM/s via `rampTick` off Heat’s RAF; BEAT/GRID are live getters so no seam), a lead-arp layer gates on at Heat>.6 (hysteresis .6/.55), **boss = MODE swap**: Phrygian-dark `Music.ALT` chord tables swapped BY REFERENCE at the bar seam (`CUR`, never mutates `CH`), reverts exactly when `state.twist` clears; quantized `stinger('bossclear')` lands on the next (brightened) bar; `duckNow` is locked out during `fadeOut` (`_fading` — a stinger can’t cancel a farewell); muted → scheduler idles (§6.6). **§2 scaled FX**: the multiply-reveal is magnitude-aware — canonical scalars `mag=log10(final+1)/5` (admission ticket) and `over=final/tithe` (surprise term), tier3=(over≥3.5&&mag≥.6)||new-turn-best, tier2=(over≥2&&mag≥.5)||capHit (B3: easy Act-1 dumps can’t fake greatness); `freeze()` hit-stop before the boom (routed through the caller’s `tw` → rush/act-speed aware; skipped in trials); ONE coalesced flashWash; chroma class on the slam (tier-3, short-eqn only); feast is CONTINUOUS (TITHE PAID→FEEDS→FEASTS→**THE FORGE GORGES**, apex string gated on mag too); **`shake()` = trauma model** — power/26 adds trauma, motion = trauma² × summed sines + up to ~2.5° rotation, `dip()` = directional downward channel; tally call-sites retuned (stars 2.5+g*1.2, relic payers 3); particle pool hard-capped 300 (`add()` shifts oldest), particle physics × `Heat.timeScale`; boss-clear/rank-up escalate by bossesCleared/rank. **§3 cards land heavier**: `cardWeight(def)=RARITY_JUICE×(1+tier·.4)×(1+min(glory,8)·.06)` — a forged rare (5.88) out-lands a vanilla mythic (4.96); `.landsquash` volume-preserving keyframe via `--sq` (common imperceptible: sq=.022·max(0,w−.9) needs w>1.13); anticipation lift −92−w·12 / hold 130+w·30ms (×juiceLevel); per-rarity bass registers (mythic/master sub-sines, forged tier rings 96→52Hz); `buyCard`/`resolveCommissions` route weight (heavier purchases thud lower; the forge slam carries the OUTPUT’s weight). **§4 big-win tiers (categorical)**: the reserved `AudioFX.chant(variant)` (transposed by turn%4 — never a ringtone) fires ONLY at tier-3/apex moments; `state.bestTurnGlory` (B2: `recordScore()` MUTATES localStorage — end-of-run only; bestTurnGlory persists in the save) → "A NEW PEAK" slam; **Tier-4 record-shatter** (glory>old best×1.5): `recordShatterFlourish()` goldbloom (desaturate-to-gold-and-rebloom) — `finishScores()` now returns `{best,shattered}` (reads prev best BEFORE the mutation); personal best is a first-class interrupt on death AND victory screens; **column of fire** `.ts-x.ablaze::before` (z-index -1 — BEHIND the glyph, digit legible, `--flame` scales with over); **victory hard-stop**: final-boss kill cuts the music (fadeOut .4), 420ms `#app.blackout` + timeScale 0 (silence), THEN the Masterwork slam + fanfare; `Firsts` (localStorage ch-sg-firsts) one-time flourishes: first boss / first mythic / first record. **§5 death feel**: filters slam shut (220/180Hz), `#app.deathgray` colour drain, the crash decays 1.4s into 420ms TRUE silence, then the 49Hz verdict; a new best removes the gray (the record outshines the death). **§6 restraint**: baseline demoted throughout; `prefers-reduced-motion` gates the WHOLE layer (shake→`#app.impulse` border pulse, freeze→0, bursts ×.3, music starts calm stays calm; colour/sound/number channels preserved); **SCREEN FX slider** in SOUND SETTINGS (`AudioFX.juice` in the ch-af-vol blob → `Heat.fxScale`); coach protection — `inTrial()` pins Heat .05, forbids freeze, `juiceLevel()` caps .35. **PLUS**: HIGHSCORE gets **THE WORLD / MY RUNS tabs** (`hsTab`/`renderHiscore()` — local = ch-af-scores top-10, race yourself); **ANALYTICS** main-menu item → `#pstats` panel (`openPlayerStats()`): 6 stat tiles (runs/wins%/best run/deepest turn·act/best single turn/win streak), most-worked wares (played/bought/forged/burned via each run’s cards.names), last-12-runs turn bars (gold=won) — from `ch-bal-log`+`ch-af-scores` ONLY, nothing leaves the machine. Tests: `v0100test.mjs` (27) + full regression battery green (pilejumptest’s mid-shake assertion updated: the trauma shake ROTATES #app so the pile RECT legitimately sways ≤8px — the invariants are layout-offset constant + exact return). **v0.9.1 = pile-jump bugfix**: v0.9.0's piles were `position:fixed` inside `#app`, and `shake()` sets a transform on `#app` — per CSS spec a transformed ancestor becomes the containing block for `position:fixed` descendants, so the piles re-anchored mid-shake and jumped horizontally, snapping back as the shake decayed. Fix: piles are now `position:absolute` and **direct children of `#app`** (moved out of `#bottom`), so they ride the shake with the board (no containing-block flip). **RULE**: a HUD element that must stay put during shake should be `position:absolute` inside the transformed `#app` (rides it) or live outside `#app` — never `position:fixed` inside a transformed ancestor. **NEXT TASK — Dopamine & Juice overhaul**: fully designed (6-agent research workflow `wf_6ae7f53b-bd7` + adversarial critique), approved (whole system, bold-tiered, evolving music), spec in `tasks/dopamine-plan.md`, handoff in `HANDOVER.md` — NOT started (user switching models to build). (v0.9.0 = six UI/balance items, 2026-07-17.) **v0.9.0 = six UI/balance items.** (1) Deck+Discard moved to the upper-right HUD (`position:fixed`, below END TURN) — a big hand fan no longer occludes them; `anchor()` reads live rects so fly-anims follow untouched. (2) Pile view gains **tabs** (All/Draw/Discard/Played — `pvTab`/`pvSource()`). (3) **ENTER ends the turn** (guarded vs typing/overlays/disabled). (4) **Late-game math fix** (the geometric-demand-vs-capped-score wall that made t42-48 need ~68k base★): demand **tapers** past the Act-3 boss (`DEMAND_KNEE=19`, rate 1.28→`DEMAND_LATE_RATE`=1.13 — Acts 1-3 byte-identical; t48 4.1M→97k, ~676 base★); the MULT cap **rises** via `effMultCap()` = 15 + 3×bossesCleared + `benchSum('capBonus')` (15→36 by the final boss); + 2 **optional stacking cap relics** (`furnaceregulator` +4 T2, `unboundforge` +8 T3, new `capBonus` field) — not mandatory since the cap already auto-rises. (5) Menu **back links** restyled from dotted-underline text to gold ghost pills (`.gm-back`). (6) **Playtime → ACTIVE time**: `Telemetry.elapsed()` pauses on tab-hidden so idle open-tab time stops inflating it (Erik's 14h55m was open-tab wall-clock incl. idle); admin relabelled "Active time". Tests: `v090test.mjs` (13). (v0.8.6 = CONTINUE market-DOM bugfix, 2026-07-17.) **v0.8.6 = CONTINUE bugfix**: v0.8.5's resume rebuilt the market DATA but not the market DOM — `restoreRun()` set `state.market` to restored slots (`slotEl:null`) while the Bazaar still showed `initMarket()`'s fresh random DOM, so the resumed board showed phantom un-clickable cards (turn/glory/deck/hand all resumed fine). Fix: `initMarket`'s DOM loop extracted into `buildMarketDOM()` (railhead split by `slot.rack`, not index), called in the `boot()` restore block after `restoreRun()`. **Test-gap lesson**: v0.8.5's CONTINUE test kept `sessionStorage` across reload, so `askName` never fired and the real reopen path was never exercised — the test now `sessionStorage.clear()`s before reload and asserts a resumed Bazaar card is BUYABLE. (v0.8.5 = run persistence + auto tally speed-up, 2026-07-17.) **v0.8.5 = the two parked playtest items.** (1) **Run persistence**: a run autosaves to localStorage `ch-sg-save` at each turn boundary (`saveRun()` before the deal); the start menu shows **CONTINUE** (primary, first — "resume · Act N, turn T, NNN★") when a save exists for this device's player, PLAY becomes "a new run". `restoreRun()` in `boot()` (after `initMarket`, guarded by `wantRestore`) rebuilds deck/discard/relics(+shattered)/market/kingdom/commissions/counters/twist; **forged cards rebuilt via `defFromId()`** (base root + `forgedDefOf` per `_f`; unknown id → dropped, graceful for stale saves). Cleared on death + RESTART, **kept on QUIT-to-menu** (leave & resume). Name-match uses the localStorage device name (session name is blank on a reopened tab). (2) **Auto tally speed-up**: `endTurn`'s `tw()` gains `actScale` — Acts 1-2 full ceremony, **Act 3+ (turn 13+) ×0.45 (~2.2× faster)**, fixing the long-late-chain drag; click-to-rush (40ms) still layers on. Tests: `v085test.mjs` (13). git committed. (v0.8.4 = playtest pass — Act-1 mercy etc., 2026-07-17.) **v0.8.4 = playtest pass.** Bot-driven playtest (real UI, 9 runs) + prod boss-death analysis found TWO walls: Boss 1 kills newcomers (Erik missed 7/16 = 44%, once 2★ short) while Hampus dies at Boss 2 (3/6, all t12). Late bosses NOT retuned — nearly all human data is pre-v0.8 cap-7; **read the ⚖ clear-rate chart for v0.8 data before touching boss demandMults.** Changes: (1) **free Act-1 first-miss mercy** (`state.act1Mercy` — "the Forge forgives an apprentice once", Act 1 only, does NOT count as clearing; softens the newcomer floor without changing mid-game; **user may pull it if it feels wrong**); (2) BUGFIX opening market ignored `afterBoss` (`pickReverie` filtered only `!deep` → Twin Pyre seeded turn 1); (3) polish: opaque game-over backdrop, MULT cap reads "CAP 15" not "/15", start-menu PLAY primary+first / LEARN "optional tutorial"; (4) security: static-file guard requires `ROOT+path.sep`. **git repo now initialized** (was untracked); v0.8.3 + v0.8.4 committed. Parked for later: run persistence + CONTINUE (session-only state loses mid-run closes), tally speed-up for long late chains. (v0.8.3 = burn confirm + admin Compare, 2026-07-16.) **v0.8.3 = burn confirm + admin Compare.** Burning now CONFIRMS: the picked card zooms to centre (#inspect overlay, `opts.burn` branch) wearing a "🔥 BURN · +N★" button; CANCEL keeps the prompt armed, SKIP BURN = spare all; both paths (hand `playCard`, pile-view `.burnbtn`) route through it, coach follows ("BURN IT"). Admin: every balance run carries `ended` → gold ⏱ duration chips on strips + drawer timelines; NEW **⇄ Compare tab** — per-player playstyle metrics from ch_run card tallies (buys/plays/forges/burns/relics per run, deck end = 12+buys−burns, max MULT from ch_turn), sortable with inline bars, **ember ring = ≥2σ outlier** (4+ tallied players) to spot abnormal tactics; client now tallies relic mounts into `cards.relics` (rack buys + master drafts — older runs show 0). aggregate.js emits `style` per player. (v0.8.2 = corner-wedge fix, 2026-07-16; v0.8.2 = card-corner wedge fix — the square-cornered .foot/.nameplate scrims + radius-0 .cardo hot-glow shadow painted past the 12px cardface radius on pile-view hover-zoom; all three now carry the matching radius, since the face can't `overflow:hidden` — badges must poke out. v0.8.1 = removed the stranded PLAYED ×N pill, a v0.3 relic orphaned by the v0.7.2 cluster move). **v0.8.0 = payouts later + Dross economy + tabbed console.** Root cause of the v0.7 "too easy": six mythics (fractal/mirrortwin/dragon/leviathan/abyss/incarnate) had NO deep/afterBoss gate — an econ start bought the ×1.5-xMult Gilded Fractal in Act 1-2. Now: leviathan/abyss `deep` (rank 2); dragon/incarnate/mirrortwin `deep+afterBoss:2`; **fractal `deep+afterBoss:3`** (xMult stays late, like the T3 master capstones). `DEMAND_BASE` 10.4→11.7 (−10% vs original 13 — half the v0.7 cut restored). **T1 Dross-for-power wares** (strong + `focusCost:1`, each play silts 1 Dross): pyrebloom (ember c4 4★), siltdredger (tide c5 3★ draw2), ashgilt (aurel c5 3★ gold2). **Burn ladder** Altar (early) → NEW **twinpyre** (uncommon c5 cantrip +1❖ burn-1, `afterBoss:1`) → Great Kiln (deep). **⚠ next balance read from the clear-rate chart**: is the early game tight again, do Acts 5-8 open up. **Forge Console = 4 hash-routed TABS** (Pulse / Difficulty / Cards / Players — page was too tall); difficulty run strips cap at 8 + "show all". Tests: v080test.mjs (gating ladder), admintest 29. **v0.7.2 = played cluster occludes less**: `#playanchor` 45%→55% + `layoutPlayed` rows 8 wide (`rows=min(3,ceil(n/8))`, scale .7/.56/.48 by row count, footprint cap 640px/50vw) — a 15-card chain is 2 short rows low on the board, not a 3-row wall across the centre; verified headless via the real `playCard` flow (15- and 27-card chains). v0.7.1 = § debug KEYBOARD-ONLY (corner tap element removed on request). **v0.7.0 = BALANCE + FORGE CONSOLE.** Balance (playtest: "20% too hard, need MULT ≥15 for late bosses"): `MULT_CAP` 7→15, `DEMAND_BASE` 13→10.4 (flat −20%; grace + boss mults untouched), and three **scaling star engines** so base ★ grows with the run instead of staying linear — `ashfall` (rare, +1★/card burned this run), `hourcrown` (mythic, +1★×turn), `worldanvil` (mythic, +1★/card played); new syn kinds `starPerBurn`/`starPerTurn` in `starsFor()`. **⚠ open design question**: demand is geometric (t48 boss ≈ 4.5M ★) — even at ×60 total mult the last acts likely stay unreachable without bending `DEMAND_RATE`; watch the new clear-rate chart and decide. **Admin = "Forge Console" redesign** (admin.html rewritten, zero-dep hand-rolled SVG): stat tiles (live-now pill, win rate, median depth), **Learn/Game funnels** (tutorial adoption vs run depth — replaces the old conflated "Furthest" column; aggregate.js now emits `learn{graduated,reached,cleared,started}` + `game{runs,won,bestTurn}`), **demand-vs-score log chart** with crosshair tooltip + **clear-rate bars**, **live event feed** (new `GET /api/admin/events?limit&cid`, 12s auto-refresh, type-group chips), honor-roll panel, card usage, and **per-player difficulty pairing**: `GET /api/admin/balance?cid=` filters runs to a merged person (via `buildCanon`/`personOf`) and the player drawer shows per-run score-vs-demand sparklines. **Player drawer = full profile** (added 2026-07-16, same day): stat strip (visits · total playtime · full/trial run counts · tutorial verdict in words · first/last seen), **session history** (`playerDetail()` now returns `sessions[]` grouped by sid — start, duration from summed `session_end` beacons else event span, per-session runs/trials/deaths/deepest turn/build), and **death locations** on both the run timelines and the finished-runs list — `deathWhere()` maps endTurn → act + boss name (t%6==0 → that act's boss, `BOSS_BY_ACT` 1-8 mirrors the game's BOSSES table) and `deathShortfall()` shows how many ★ short the final turn was. **Magnitude encodings** (same day): run sparklines use a fixed 5.5px/turn (`SPARK_PX_PER_TURN` — length = depth, directly comparable), an 8-cell act meter (`actMeter()`, half-cell = died mid-act, green = Forged) rides on drawer runs / session rows / the table's Game column, and proportional time-bars back session rows + playtime cells. Chart palette `#2f8fe6/#b8880f/#2fae5f/#e0492a/#9a55e8` validated (dataviz six checks) on surface `#130d1d`. Tests: `admintest.mjs` (18 asserts, seeded server + headless console drive, 3× stable), `baltest.mjs` (caps/cards/overflow), plus the v0.6.9 battery. **v0.6.9 (ships inside v0.7.0)**: pile-view recede fixed — dim/pop now via JS classes (`.receding`/`.hot`) keyed to the card under the pointer, not container `:hover` (pointer in the gaps/empty flanks used to dim ALL cards with none popped, and the dim stuck after forge re-renders); **§ secret debug** — the § key (keyboard-only as of v0.7.1) opens a copyable JSON state dump (build/run/piles/score/overlays/ch-* storage/error ring buffer `window.__sgErrLog`; handle `window.__sgDump()`) for paste-into-bug-report live debugging. **v0.6.8 = SHARED honor roll**: run scores POST to `/api/score` (new route in `server.js` beside `/api/t`; validation via `clampStr`/`clampInt`, `MAX_BODY` 8KB) and both the game-over board and the HIGHSCORE panel swap in the global top-10 from `GET /api/scores?cid=` once the server answers (render-local-then-swap — the ceremony never waits on the network; offline/`:5713`/`file://` keep today's pure-localStorage behavior via the `Telemetry.off` gate). Own rows highlighted by a server-computed `me` flag (`shapeTop()` strips `cid`/`rid`/`ts` so player ids never leave the server; reuses `.hs-row.me` CSS). Persistence: `scores.json` on the `/data` volume via `store.js addScore()/topScores()` — kept sorted, capped at 100, write-then-rename. **Dedup**: client sends `rid = Telemetry.sid` (per page load); same `rid` is replaced only by a higher total, so a win that rolls into an Endless death holds ONE slot at its peak (every restart goes through `location.reload()` → fresh rid). The two duplicated record+render blocks in `runVictory`/`gameOverSequence` were folded into one `finishScores()`. Tests (all green, session scratchpad): `scorestest.mjs` — 46 API asserts × in-memory + DATA_DIR backends incl. restart persistence; `hiscoretest.mjs` — 12 in-browser asserts via chrome-headless-shell (two browser contexts = two cids: B sees A's run unhighlighted; game-over POST + panel swap + `.me`; `:5713` fires zero `/api/*` requests). **v0.6.7 = test-player fixes**: (1) **Boon click-guard** — a Boon is a FREE reward, so a stray backdrop click no longer discards it; only clicking a card or DECLINE dismisses (removed the `#boon` backdrop `pointerdown` decline listener, mirroring the mandatory `#mrdraft` draft). (2) **Score-reveal overshoot + legible MULT cap** — the end-turn MULT reveal painted its *uncapped* running additive sum (e.g. `×58 → 13456`) and only clamped afterward, snapping down to the true `×14 = 3262`; now the two additive-phase `show()` paints are clamped to `MULT_CAP` (via a `showMult` helper) so the meter climbs only to the real capped total. AND the ×7 additive cap is now legible (test player didn't know it existed): `computeScore()` returns `add`/`x`; the tithe MULT slot shows the ADDITIVE mult with a persistent `/7` denominator that flips to a gold **MAX** when `add>=MULT_CAP`, a `★ MULT MAXED · ×7` slam fires the instant the reveal hits the cap, and the keystone (× bucket) rides as its own `⚡×N` chip so `/7` stays truthful. Verified end-to-end in-browser (backdrop inert / DECLINE+card work; over-cap board → `OVERSHOOT: NONE`, committed glory 56, meter `4★ × 7 MAX ⚡×2 = 56★`). Analytics ask (#3) answered, build deferred: per-turn scores already ride `ch_turn` (cid+demand+base+mult+score) and show in `/admin` → "⚖ Difficulty" (global, unfiltered); a per-player filter (`?cid=` on `/api/admin/balance` + timeline in `openDetail`) is the deferred enhancement. (Prior deploy: v0.6.6, 2026-07-14.) **v0.6.6 = pile-view hover-zoom corner artifacts fixed**: hovering a card now recedes its neighbours (`#pv-grid-inner:hover .pv-slot{opacity:.38}`, hovered slot back to 1) so a neighbour's gold FORGE button / count badge no longer peeks past the enlarged card's corner (the "yellow corner"); the popped card wears a *tighter* gold lift-shadow (`0 20px 40px -14px rgba(0,0,0,.6)` — negative spread hugs the card instead of pooling a dark box) and a softer count-badge drop (`#pileview .pilecount`), killing the "black box" at the top-right. Root cause was empirically pinned with `elementsFromPoint` (bottom-left corner returned the *neighbour's* `.forgebtn`, not a card layer) + a cyan-flood test (the dark halo was the hover-only `.cardo` box-shadow). Cardback was ruled out (backface-visibility works; hidden-vs-visible screenshots identical). (Prior deploy: v0.6.5, 2026-07-14.) v0.6.4: PLAY open from first visit (Learn-before-Play lock removed, LEARN now an optional tutorial). **v0.6.5 = four pile-view refinements**: (1) hover-zoom no longer clips an edge card — `#pv-grid` stays `overflow:visible` while the deck fits so a popped card spills past the padding uncropped; `fitPileView` adds a `.scroll` overflow box only on genuine overflow, decided from live geometry `scrollHeight>clientHeight` (NOT the gap-unaware `best`, which under-reports height because CSS `zoom` scales the 14px gaps too), plus a `document.fonts.ready` re-fit; (2) cards vertically centred when the deck fits (`#pv-grid` `align-items:center`, reverting to `flex-start` under `.scroll` so a tall deck's top row stays reachable); (3) removed the redundant forge-preview eye button (`.forgeeye`/`ICONS.eye`); (4) dropped the dark card-name scrim in the pile view (`#pileview .nameplate{background:none}`) that magnified into a black frame — the `.cname` text-shadow keeps titles legible. `index.html` IS the chapters game (8-act progression run, monotonic demand, Focus/Forge/Dross economy). `chapters.html` is the identical working copy the build was developed in — keep the two in sync (edit one, `cp` to the other) until `chapters.html` is retired. The pre-chapters StarGilt is archived at `scratchpad/index.OLD-stargilt.html`.
- **Custom domain — https://www.stargilt.com is LIVE** (fixed 2026-07-14; serves the game behind the Mellon gate, valid TLS). Bought on Cloudflare (zone id `25f89dc4827ba704ece1647426989b6f`; token in `~/.claude/api-tokens.json.cloudflare`, DNS + zone-settings scope only). **Apex `stargilt.com` → 301 → www is LIVE** (2026-07-16, verified http+https, path+query preserved): the apex can never be a Railway domain (a bare apex on Cloudflare can't validate — CNAME-flattening hides the record from Railway, and the workspace domain cap is full), so it redirects at the Cloudflare edge. Two pieces: (1) proxied placeholder `A stargilt.com → 192.0.2.1` (TEST-NET dummy, orange cloud — the edge answers, traffic never reaches the IP; created via API); (2) Single Redirect rule "Redirect from root to WWW [Template]": `http.host eq "stargilt.com"` → 301 dynamic `concat("https://www.stargilt.com", http.request.uri.path)` + preserve query — created via the **dashboard** (Rules → Overview → template, then edited: the template's `https://stargilt.com/*` wildcard misses plain-http requests; the `http.host` expression catches both schemes). Neither API token could write it: rulesets writes need **Zone → Dynamic URL Redirects: Edit**, which the DNS token (`cloudflare.token`, `cfut_…`, verify at `/user/tokens/verify`) and the account-owned token (`cloudflare.account_token`, `cfat_…`, verify at `/accounts/{id}/tokens/verify`) both lack. Two Cloudflare DNS records make it work, both DNS-only (grey cloud): `CNAME www → 78fxcrps.up.railway.app` **and** `TXT _railway-verify.www → railway-verify=4514ea0abb6cd8865e62a58d5272fbc569a5b336bbfdc86f9708dc8a654ce2bc`. **The TXT ownership record was the real blocker**: Railway won't install the edge route without it and returns `{"code":404,"message":"Application not found"}` until it's present — and the GraphQL `domains(){customDomains{status{dnsRecords}}}` API only surfaces the CNAME, never the TXT, so the API looked green for days while the site 404'd. The TXT requirement is only visible in the Railway **dashboard** → service → Settings → Networking → the domain's "Show DNS records" modal. (The cert `VALIDATING_OWNERSHIP` stall along the way was separately fixed by the `customDomainIssueCertificate(id)` mutation — NOT by delete/re-add churn, which mints a new CNAME target each time and risks LE rate limits.) See memory `railway-custom-domain-playbook`. IDs: project `ee4df6cc-…`, env `143b7f24-…`, service `888336c0-…`. Workspace custom-domain cap = 2 (www.stargilt.com + terminaltodo.com).
- **Playtest gate ("Mellon")**: the game pages (`/`, `/index.html`, `/chapters.html`) are behind a **server-side** password gate in `server.js` — a themed "Speak, friend, and enter" (Doors of Durin) page. Password = `process.env.SG_GATE` (default `'Mellon'`; set `SG_GATE=''` to disable). Correct word → `sg_gate` cookie (sha256 token, 90-day) → game. `/api/*` and `/admin` (own SG_ADMIN_KEY) are NOT behind it. The game is never sent to an un-authed visitor. (An earlier client-side "MELLON invite button" was a misread of the ask and was removed.)
- **v0.6.2** (deep-sweep round): full-screen pile view (`fitPileView` — continuous fit-zoom on `#pv-grid-inner`, replaces the old dense/vdense steps); Ledger of the Vale `handGold:3`; **card-usage telemetry** (Balance module tallies buys/plays/forges/burns per run → `ch_run.cards` + `ch_cards` pagehide flush → `/api/admin/cards` → admin "⚒ Card Usage" panel); Learn-gate enforced (`hasLearned` checks `ch-sg-learned==='1'`); Mellon invite; + sweep fixes: tally-reveal math now mirrors `computeScore` (multDouble `+=3`, comboX2 `.12`, MULT_CAP/X_CAP clamps), dead-stat relics converted to multiplied levers (firstflame handStars:5, trophy relicMult:.3, brokerswrit +handGold:2, longkiln +handStars:1, moltenledger gold→3), multi-field relic text, emoji removed from chrome (🔥 burn toast, ⚒/⁂ FORGE), victory `.win` gold palette, coach z-index above modals, HUD/inspector/anno responsive, draw→teal color, slam font→Fraunces. **Deferred to user**: ⚡ synergy-mark emoji→SVG (22 sites, risky), burn-confirm friction.
- **Local dev**: `python3 -m http.server 5713` from project root → http://localhost:5713
  (port 5199 is taken by another project's Vite server — do not use). For /admin + telemetry locally: `SG_ADMIN_KEY=… node server.js` → http://localhost:3000 (telemetry & balance beacons flow on every host except file:// and localhost:5713).
- **No build step**: single self-contained `index.html` (Google Fonts is the only network dependency); also works via `file://`
- **Deploy**: https://stargilt-production.up.railway.app — Railway project `stargilt`; redeploy with `railway up --detach --service stargilt` from project root (server.js; .railwayignore excludes tasks/, CONTEXT.md, node_modules/)
- **Difficulty knobs** (chapters): `DEMAND_BASE=13`, `DEMAND_RATE=1.28`, `EARLY_GRACE=9`, `GRACE_FLOOR=0.5` (variant C — eases t1-t8), boss `demandMult`s 1.3→3.2, `MULT_CAP=7`, `X_CAP=4`. Base focus 1 (`focusPerTurn=1+floor(rank/3)`). Balance track (`ch_turn`/`ch_run`) → /admin ⚖ Difficulty panel, tagged by BUILD.
- **Analytics (v28)**: `server.js` (zero-dep Node http) also ingests player telemetry (`/api/t`) and serves the admin console at `/admin`. `store.js` = JSON files on the Railway volume at `/data` (env `DATA_DIR`), in-memory locally. Admin gated by env `SG_ADMIN_KEY`. Client hooks + `sg-cid` live in index.html (a `Telemetry` module, gated OFF on localhost:5713 so tests stay clean). `aggregate.js` is pure. Tests: `apitest.mjs`, `filestore.mjs`, `analytics.mjs` in the scratchpad. See memory `analytics-architecture`.
- **Tests**: headless scripts in the session scratchpad — `cardaudit.mjs` renders every card def at every size/state and fails on any overflow: run it after ANY card or CSS change.
  Main suites (shoot/tut/race/tension/synergy .mjs), puppeteer-core against the local server.
  **Use the `chrome-headless-shell` binary, not full headless Chrome** — full headless intermittently freezes its compositor on this page (screenshots time out, real-pointer clicks hit-test against stale frames). `window.__af` (state) and `window.__afGive(id)` (deal a card) are exposed for tests.
- **Task log**: `tasks/todo.md`
- **Difficulty tuning**: `TITHE_FOR` in index.html (base 6, ×1.28/turn — r15) + Mercy (one forgiven failure/run) + escalating Boon thresholds
- **Scoring**: `computeScore()` is the single authority — one currency: `round(★ × MULT) + ★-from-hand` (Sparks ✦ removed in r13; struck ★ live in `state.bonusStars`, ⚡ fires in `state.synFires`); the meter projection must always equal the tally — `strategy.mjs`/`contract.mjs`/`balance.mjs` assert this
- **Design doc**: approved plan at `~/.claude/plans/the-dopamine-slot-machine-is-quizzical-gadget.md` (Dominion methods → Focus/Buys/relics/Furnace/Reverie)
