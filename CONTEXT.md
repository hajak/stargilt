# StarGilt (né Aetherforge) — deckbuilder

- **Live game = the Chapters build** (promoted 2026-07-12). **LIVE = `chapters · v0.9.0`** (deployed 2026-07-17). **v0.9.0 = six UI/balance items.** (1) Deck+Discard moved to the upper-right HUD (`position:fixed`, below END TURN) — a big hand fan no longer occludes them; `anchor()` reads live rects so fly-anims follow untouched. (2) Pile view gains **tabs** (All/Draw/Discard/Played — `pvTab`/`pvSource()`). (3) **ENTER ends the turn** (guarded vs typing/overlays/disabled). (4) **Late-game math fix** (the geometric-demand-vs-capped-score wall that made t42-48 need ~68k base★): demand **tapers** past the Act-3 boss (`DEMAND_KNEE=19`, rate 1.28→`DEMAND_LATE_RATE`=1.13 — Acts 1-3 byte-identical; t48 4.1M→97k, ~676 base★); the MULT cap **rises** via `effMultCap()` = 15 + 3×bossesCleared + `benchSum('capBonus')` (15→36 by the final boss); + 2 **optional stacking cap relics** (`furnaceregulator` +4 T2, `unboundforge` +8 T3, new `capBonus` field) — not mandatory since the cap already auto-rises. (5) Menu **back links** restyled from dotted-underline text to gold ghost pills (`.gm-back`). (6) **Playtime → ACTIVE time**: `Telemetry.elapsed()` pauses on tab-hidden so idle open-tab time stops inflating it (Erik's 14h55m was open-tab wall-clock incl. idle); admin relabelled "Active time". Tests: `v090test.mjs` (13). (v0.8.6 = CONTINUE market-DOM bugfix, 2026-07-17.) **v0.8.6 = CONTINUE bugfix**: v0.8.5's resume rebuilt the market DATA but not the market DOM — `restoreRun()` set `state.market` to restored slots (`slotEl:null`) while the Bazaar still showed `initMarket()`'s fresh random DOM, so the resumed board showed phantom un-clickable cards (turn/glory/deck/hand all resumed fine). Fix: `initMarket`'s DOM loop extracted into `buildMarketDOM()` (railhead split by `slot.rack`, not index), called in the `boot()` restore block after `restoreRun()`. **Test-gap lesson**: v0.8.5's CONTINUE test kept `sessionStorage` across reload, so `askName` never fired and the real reopen path was never exercised — the test now `sessionStorage.clear()`s before reload and asserts a resumed Bazaar card is BUYABLE. (v0.8.5 = run persistence + auto tally speed-up, 2026-07-17.) **v0.8.5 = the two parked playtest items.** (1) **Run persistence**: a run autosaves to localStorage `ch-sg-save` at each turn boundary (`saveRun()` before the deal); the start menu shows **CONTINUE** (primary, first — "resume · Act N, turn T, NNN★") when a save exists for this device's player, PLAY becomes "a new run". `restoreRun()` in `boot()` (after `initMarket`, guarded by `wantRestore`) rebuilds deck/discard/relics(+shattered)/market/kingdom/commissions/counters/twist; **forged cards rebuilt via `defFromId()`** (base root + `forgedDefOf` per `_f`; unknown id → dropped, graceful for stale saves). Cleared on death + RESTART, **kept on QUIT-to-menu** (leave & resume). Name-match uses the localStorage device name (session name is blank on a reopened tab). (2) **Auto tally speed-up**: `endTurn`'s `tw()` gains `actScale` — Acts 1-2 full ceremony, **Act 3+ (turn 13+) ×0.45 (~2.2× faster)**, fixing the long-late-chain drag; click-to-rush (40ms) still layers on. Tests: `v085test.mjs` (13). git committed. (v0.8.4 = playtest pass — Act-1 mercy etc., 2026-07-17.) **v0.8.4 = playtest pass.** Bot-driven playtest (real UI, 9 runs) + prod boss-death analysis found TWO walls: Boss 1 kills newcomers (Erik missed 7/16 = 44%, once 2★ short) while Hampus dies at Boss 2 (3/6, all t12). Late bosses NOT retuned — nearly all human data is pre-v0.8 cap-7; **read the ⚖ clear-rate chart for v0.8 data before touching boss demandMults.** Changes: (1) **free Act-1 first-miss mercy** (`state.act1Mercy` — "the Forge forgives an apprentice once", Act 1 only, does NOT count as clearing; softens the newcomer floor without changing mid-game; **user may pull it if it feels wrong**); (2) BUGFIX opening market ignored `afterBoss` (`pickReverie` filtered only `!deep` → Twin Pyre seeded turn 1); (3) polish: opaque game-over backdrop, MULT cap reads "CAP 15" not "/15", start-menu PLAY primary+first / LEARN "optional tutorial"; (4) security: static-file guard requires `ROOT+path.sep`. **git repo now initialized** (was untracked); v0.8.3 + v0.8.4 committed. Parked for later: run persistence + CONTINUE (session-only state loses mid-run closes), tally speed-up for long late chains. (v0.8.3 = burn confirm + admin Compare, 2026-07-16.) **v0.8.3 = burn confirm + admin Compare.** Burning now CONFIRMS: the picked card zooms to centre (#inspect overlay, `opts.burn` branch) wearing a "🔥 BURN · +N★" button; CANCEL keeps the prompt armed, SKIP BURN = spare all; both paths (hand `playCard`, pile-view `.burnbtn`) route through it, coach follows ("BURN IT"). Admin: every balance run carries `ended` → gold ⏱ duration chips on strips + drawer timelines; NEW **⇄ Compare tab** — per-player playstyle metrics from ch_run card tallies (buys/plays/forges/burns/relics per run, deck end = 12+buys−burns, max MULT from ch_turn), sortable with inline bars, **ember ring = ≥2σ outlier** (4+ tallied players) to spot abnormal tactics; client now tallies relic mounts into `cards.relics` (rack buys + master drafts — older runs show 0). aggregate.js emits `style` per player. (v0.8.2 = corner-wedge fix, 2026-07-16; v0.8.2 = card-corner wedge fix — the square-cornered .foot/.nameplate scrims + radius-0 .cardo hot-glow shadow painted past the 12px cardface radius on pile-view hover-zoom; all three now carry the matching radius, since the face can't `overflow:hidden` — badges must poke out. v0.8.1 = removed the stranded PLAYED ×N pill, a v0.3 relic orphaned by the v0.7.2 cluster move). **v0.8.0 = payouts later + Dross economy + tabbed console.** Root cause of the v0.7 "too easy": six mythics (fractal/mirrortwin/dragon/leviathan/abyss/incarnate) had NO deep/afterBoss gate — an econ start bought the ×1.5-xMult Gilded Fractal in Act 1-2. Now: leviathan/abyss `deep` (rank 2); dragon/incarnate/mirrortwin `deep+afterBoss:2`; **fractal `deep+afterBoss:3`** (xMult stays late, like the T3 master capstones). `DEMAND_BASE` 10.4→11.7 (−10% vs original 13 — half the v0.7 cut restored). **T1 Dross-for-power wares** (strong + `focusCost:1`, each play silts 1 Dross): pyrebloom (ember c4 4★), siltdredger (tide c5 3★ draw2), ashgilt (aurel c5 3★ gold2). **Burn ladder** Altar (early) → NEW **twinpyre** (uncommon c5 cantrip +1❖ burn-1, `afterBoss:1`) → Great Kiln (deep). **⚠ next balance read from the clear-rate chart**: is the early game tight again, do Acts 5-8 open up. **Forge Console = 4 hash-routed TABS** (Pulse / Difficulty / Cards / Players — page was too tall); difficulty run strips cap at 8 + "show all". Tests: v080test.mjs (gating ladder), admintest 29. **v0.7.2 = played cluster occludes less**: `#playanchor` 45%→55% + `layoutPlayed` rows 8 wide (`rows=min(3,ceil(n/8))`, scale .7/.56/.48 by row count, footprint cap 640px/50vw) — a 15-card chain is 2 short rows low on the board, not a 3-row wall across the centre; verified headless via the real `playCard` flow (15- and 27-card chains). v0.7.1 = § debug KEYBOARD-ONLY (corner tap element removed on request). **v0.7.0 = BALANCE + FORGE CONSOLE.** Balance (playtest: "20% too hard, need MULT ≥15 for late bosses"): `MULT_CAP` 7→15, `DEMAND_BASE` 13→10.4 (flat −20%; grace + boss mults untouched), and three **scaling star engines** so base ★ grows with the run instead of staying linear — `ashfall` (rare, +1★/card burned this run), `hourcrown` (mythic, +1★×turn), `worldanvil` (mythic, +1★/card played); new syn kinds `starPerBurn`/`starPerTurn` in `starsFor()`. **⚠ open design question**: demand is geometric (t48 boss ≈ 4.5M ★) — even at ×60 total mult the last acts likely stay unreachable without bending `DEMAND_RATE`; watch the new clear-rate chart and decide. **Admin = "Forge Console" redesign** (admin.html rewritten, zero-dep hand-rolled SVG): stat tiles (live-now pill, win rate, median depth), **Learn/Game funnels** (tutorial adoption vs run depth — replaces the old conflated "Furthest" column; aggregate.js now emits `learn{graduated,reached,cleared,started}` + `game{runs,won,bestTurn}`), **demand-vs-score log chart** with crosshair tooltip + **clear-rate bars**, **live event feed** (new `GET /api/admin/events?limit&cid`, 12s auto-refresh, type-group chips), honor-roll panel, card usage, and **per-player difficulty pairing**: `GET /api/admin/balance?cid=` filters runs to a merged person (via `buildCanon`/`personOf`) and the player drawer shows per-run score-vs-demand sparklines. **Player drawer = full profile** (added 2026-07-16, same day): stat strip (visits · total playtime · full/trial run counts · tutorial verdict in words · first/last seen), **session history** (`playerDetail()` now returns `sessions[]` grouped by sid — start, duration from summed `session_end` beacons else event span, per-session runs/trials/deaths/deepest turn/build), and **death locations** on both the run timelines and the finished-runs list — `deathWhere()` maps endTurn → act + boss name (t%6==0 → that act's boss, `BOSS_BY_ACT` 1-8 mirrors the game's BOSSES table) and `deathShortfall()` shows how many ★ short the final turn was. **Magnitude encodings** (same day): run sparklines use a fixed 5.5px/turn (`SPARK_PX_PER_TURN` — length = depth, directly comparable), an 8-cell act meter (`actMeter()`, half-cell = died mid-act, green = Forged) rides on drawer runs / session rows / the table's Game column, and proportional time-bars back session rows + playtime cells. Chart palette `#2f8fe6/#b8880f/#2fae5f/#e0492a/#9a55e8` validated (dataviz six checks) on surface `#130d1d`. Tests: `admintest.mjs` (18 asserts, seeded server + headless console drive, 3× stable), `baltest.mjs` (caps/cards/overflow), plus the v0.6.9 battery. **v0.6.9 (ships inside v0.7.0)**: pile-view recede fixed — dim/pop now via JS classes (`.receding`/`.hot`) keyed to the card under the pointer, not container `:hover` (pointer in the gaps/empty flanks used to dim ALL cards with none popped, and the dim stuck after forge re-renders); **§ secret debug** — the § key (keyboard-only as of v0.7.1) opens a copyable JSON state dump (build/run/piles/score/overlays/ch-* storage/error ring buffer `window.__sgErrLog`; handle `window.__sgDump()`) for paste-into-bug-report live debugging. **v0.6.8 = SHARED honor roll**: run scores POST to `/api/score` (new route in `server.js` beside `/api/t`; validation via `clampStr`/`clampInt`, `MAX_BODY` 8KB) and both the game-over board and the HIGHSCORE panel swap in the global top-10 from `GET /api/scores?cid=` once the server answers (render-local-then-swap — the ceremony never waits on the network; offline/`:5713`/`file://` keep today's pure-localStorage behavior via the `Telemetry.off` gate). Own rows highlighted by a server-computed `me` flag (`shapeTop()` strips `cid`/`rid`/`ts` so player ids never leave the server; reuses `.hs-row.me` CSS). Persistence: `scores.json` on the `/data` volume via `store.js addScore()/topScores()` — kept sorted, capped at 100, write-then-rename. **Dedup**: client sends `rid = Telemetry.sid` (per page load); same `rid` is replaced only by a higher total, so a win that rolls into an Endless death holds ONE slot at its peak (every restart goes through `location.reload()` → fresh rid). The two duplicated record+render blocks in `runVictory`/`gameOverSequence` were folded into one `finishScores()`. Tests (all green, session scratchpad): `scorestest.mjs` — 46 API asserts × in-memory + DATA_DIR backends incl. restart persistence; `hiscoretest.mjs` — 12 in-browser asserts via chrome-headless-shell (two browser contexts = two cids: B sees A's run unhighlighted; game-over POST + panel swap + `.me`; `:5713` fires zero `/api/*` requests). **v0.6.7 = test-player fixes**: (1) **Boon click-guard** — a Boon is a FREE reward, so a stray backdrop click no longer discards it; only clicking a card or DECLINE dismisses (removed the `#boon` backdrop `pointerdown` decline listener, mirroring the mandatory `#mrdraft` draft). (2) **Score-reveal overshoot + legible MULT cap** — the end-turn MULT reveal painted its *uncapped* running additive sum (e.g. `×58 → 13456`) and only clamped afterward, snapping down to the true `×14 = 3262`; now the two additive-phase `show()` paints are clamped to `MULT_CAP` (via a `showMult` helper) so the meter climbs only to the real capped total. AND the ×7 additive cap is now legible (test player didn't know it existed): `computeScore()` returns `add`/`x`; the tithe MULT slot shows the ADDITIVE mult with a persistent `/7` denominator that flips to a gold **MAX** when `add>=MULT_CAP`, a `★ MULT MAXED · ×7` slam fires the instant the reveal hits the cap, and the keystone (× bucket) rides as its own `⚡×N` chip so `/7` stays truthful. Verified end-to-end in-browser (backdrop inert / DECLINE+card work; over-cap board → `OVERSHOOT: NONE`, committed glory 56, meter `4★ × 7 MAX ⚡×2 = 56★`). Analytics ask (#3) answered, build deferred: per-turn scores already ride `ch_turn` (cid+demand+base+mult+score) and show in `/admin` → "⚖ Difficulty" (global, unfiltered); a per-player filter (`?cid=` on `/api/admin/balance` + timeline in `openDetail`) is the deferred enhancement. (Prior deploy: v0.6.6, 2026-07-14.) **v0.6.6 = pile-view hover-zoom corner artifacts fixed**: hovering a card now recedes its neighbours (`#pv-grid-inner:hover .pv-slot{opacity:.38}`, hovered slot back to 1) so a neighbour's gold FORGE button / count badge no longer peeks past the enlarged card's corner (the "yellow corner"); the popped card wears a *tighter* gold lift-shadow (`0 20px 40px -14px rgba(0,0,0,.6)` — negative spread hugs the card instead of pooling a dark box) and a softer count-badge drop (`#pileview .pilecount`), killing the "black box" at the top-right. Root cause was empirically pinned with `elementsFromPoint` (bottom-left corner returned the *neighbour's* `.forgebtn`, not a card layer) + a cyan-flood test (the dark halo was the hover-only `.cardo` box-shadow). Cardback was ruled out (backface-visibility works; hidden-vs-visible screenshots identical). (Prior deploy: v0.6.5, 2026-07-14.) v0.6.4: PLAY open from first visit (Learn-before-Play lock removed, LEARN now an optional tutorial). **v0.6.5 = four pile-view refinements**: (1) hover-zoom no longer clips an edge card — `#pv-grid` stays `overflow:visible` while the deck fits so a popped card spills past the padding uncropped; `fitPileView` adds a `.scroll` overflow box only on genuine overflow, decided from live geometry `scrollHeight>clientHeight` (NOT the gap-unaware `best`, which under-reports height because CSS `zoom` scales the 14px gaps too), plus a `document.fonts.ready` re-fit; (2) cards vertically centred when the deck fits (`#pv-grid` `align-items:center`, reverting to `flex-start` under `.scroll` so a tall deck's top row stays reachable); (3) removed the redundant forge-preview eye button (`.forgeeye`/`ICONS.eye`); (4) dropped the dark card-name scrim in the pile view (`#pileview .nameplate{background:none}`) that magnified into a black frame — the `.cname` text-shadow keeps titles legible. `index.html` IS the chapters game (8-act progression run, monotonic demand, Focus/Forge/Dross economy). `chapters.html` is the identical working copy the build was developed in — keep the two in sync (edit one, `cp` to the other) until `chapters.html` is retired. The pre-chapters StarGilt is archived at `scratchpad/index.OLD-stargilt.html`.
- **Custom domain — https://www.stargilt.com is LIVE** (fixed 2026-07-14; serves the game behind the Mellon gate, valid TLS). Bought on Cloudflare (zone id `25f89dc4827ba704ece1647426989b6f`; token in `~/.claude/api-tokens.json.cloudflare`, DNS + zone-settings scope only). **Apex `stargilt.com` → 301 → www is LIVE** (2026-07-16, verified http+https, path+query preserved): the apex can never be a Railway domain (a bare apex on Cloudflare can't validate — CNAME-flattening hides the record from Railway, and the workspace domain cap is full), so it redirects at the Cloudflare edge. Two pieces: (1) proxied placeholder `A stargilt.com → 192.0.2.1` (TEST-NET dummy, orange cloud — the edge answers, traffic never reaches the IP; created via API); (2) Single Redirect rule "Redirect from root to WWW [Template]": `http.host eq "stargilt.com"` → 301 dynamic `concat("https://www.stargilt.com", http.request.uri.path)` + preserve query — created via the **dashboard** (Rules → Overview → template, then edited: the template's `https://stargilt.com/*` wildcard misses plain-http requests; the `http.host` expression catches both schemes). Neither API token could write it: rulesets writes need **Zone → Dynamic URL Redirects: Edit**, which the DNS token (`cloudflare.token`, `cfut_…`, verify at `/user/tokens/verify`) and the account-owned token (`cloudflare.account_token`, `cfat_…`, verify at `/accounts/{id}/tokens/verify`) both lack. Two Cloudflare DNS records make it work, both DNS-only (grey cloud): `CNAME www → 78fxcrps.up.railway.app` **and** `TXT _railway-verify.www → railway-verify=4514ea0abb6cd8865e62a58d5272fbc569a5b336bbfdc86f9708dc8a654ce2bc`. **The TXT ownership record was the real blocker**: Railway won't install the edge route without it and returns `{"code":404,"message":"Application not found"}` until it's present — and the GraphQL `domains(){customDomains{status{dnsRecords}}}` API only surfaces the CNAME, never the TXT, so the API looked green for days while the site 404'd. The TXT requirement is only visible in the Railway **dashboard** → service → Settings → Networking → the domain's "Show DNS records" modal. (The cert `VALIDATING_OWNERSHIP` stall along the way was separately fixed by the `customDomainIssueCertificate(id)` mutation — NOT by delete/re-add churn, which mints a new CNAME target each time and risks LE rate limits.) See memory `railway-custom-domain-playbook`. IDs: project `ee4df6cc-…`, env `143b7f24-…`, service `888336c0-…`. Workspace custom-domain cap = 2 (www.stargilt.com + terminaltodo.com).
- **Playtest gate ("Mellon")**: the game pages (`/`, `/index.html`, `/chapters.html`) are behind a **server-side** password gate in `server.js` — a themed "Speak, friend, and enter" (Doors of Durin) page. Password = `process.env.SG_GATE` (default `'Mellon'`; set `SG_GATE=''` to disable). Correct word → `sg_gate` cookie (sha256 token, 90-day) → game. `/api/*` and `/admin` (own SG_ADMIN_KEY) are NOT behind it. The game is never sent to an un-authed visitor. (An earlier client-side "MELLON invite button" was a misread of the ask and was removed.)
- **v0.6.2** (deep-sweep round): full-screen pile view (`fitPileView` — continuous fit-zoom on `#pv-grid-inner`, replaces the old dense/vdense steps); Ledger of the Vale `handGold:3`; **card-usage telemetry** (Balance module tallies buys/plays/forges/burns per run → `ch_run.cards` + `ch_cards` pagehide flush → `/api/admin/cards` → admin "⚒ Card Usage" panel); Learn-gate enforced (`hasLearned` checks `ch-sg-learned==='1'`); Mellon invite; + sweep fixes: tally-reveal math now mirrors `computeScore` (multDouble `+=3`, comboX2 `.12`, MULT_CAP/X_CAP clamps), dead-stat relics converted to multiplied levers (firstflame handStars:5, trophy relicMult:.3, brokerswrit +handGold:2, longkiln +handStars:1, moltenledger gold→3), multi-field relic text, emoji removed from chrome (🔥 burn toast, ⚒/⁂ FORGE), victory `.win` gold palette, coach z-index above modals, HUD/inspector/anno responsive, draw→teal color, slam font→Fraunces. **Deferred to user**: ⚡ synergy-mark emoji→SVG (22 sites, risky), burn-confirm friction.
- **Local dev**: `python3 -m http.server 5713` from project root → http://localhost:5713
  (port 5199 is taken by another project's Vite server — do not use). For /admin + telemetry locally: `SG_ADMIN_KEY=… node server.js` → http://localhost:3000 (telemetry & balance beacons flow on every host except file:// and localhost:5713).
- **No build step**: single self-contained `index.html` (Google Fonts is the only network dependency); also works via `file://`
- **Deploy**: https://stargilt-production.up.railway.app — Railway project `stargilt`; redeploy with `railway up --detach --service stargilt` from project root (server.js; .railwayignore excludes tasks/, CONTEXT.md, node_modules/)
- **Difficulty knobs** (chapters): `DEMAND_BASE=13`, `DEMAND_RATE=1.28`, `EARLY_GRACE=9`, `GRACE_FLOOR=0.5` (variant C — eases t1-t8), boss `demandMult`s 1.3→3.2, `MULT_CAP=7`, `X_CAP=4`. Base focus 1 (`focusPerTurn=1+floor(rank/3)`). Balance track (`ch_turn`/`ch_run`) → /admin ⚖ Difficulty panel, tagged by BUILD.
- **Analytics (v28)**: `server.js` (zero-dep Node http) also ingests player telemetry (`/api/t`) and serves the admin console at `/admin`. `store.js` = JSON files on the Railway volume at `/data` (env `DATA_DIR`), in-memory locally. Admin gated by env `SG_ADMIN_KEY`. Client hooks + `sg-cid` live in index.html (a `Telemetry` module, gated OFF on localhost:5713 so tests stay clean). `aggregate.js` is pure. Tests: `apitest.mjs`, `filestore.mjs`, `analytics.mjs` in the scratchpad. See memory `analytics-architecture`.
- **Tests**: headless scripts in the session scratchpad — `cardaudit.mjs` renders every card def at every size/state and fails on any overflow: run it after ANY card or CSS change.
  Main suites (shoot/tut/race/tension/synergy .mjs), puppeteer-core against the local server.
  **Use the `chrome-headless-shell` binary, not full headless Chrome** — full headless intermittently freezes its compositor on this page (screenshots time out, real-pointer clicks hit-test against stale frames). `window.__af` (state) and `window.__afGive(id)` (deal a card) are exposed for tests.
- **Task log**: `tasks/todo.md`
- **Difficulty tuning**: `TITHE_FOR` in index.html (base 6, ×1.28/turn — r15) + Mercy (one forgiven failure/run) + escalating Boon thresholds
- **Scoring**: `computeScore()` is the single authority — one currency: `round(★ × MULT) + ★-from-hand` (Sparks ✦ removed in r13; struck ★ live in `state.bonusStars`, ⚡ fires in `state.synFires`); the meter projection must always equal the tally — `strategy.mjs`/`contract.mjs`/`balance.mjs` assert this
- **Design doc**: approved plan at `~/.claude/plans/the-dopamine-slot-machine-is-quizzical-gadget.md` (Dominion methods → Focus/Buys/relics/Furnace/Reverie)
